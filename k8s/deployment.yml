apiVersion: apps/v1
kind: Deployment
metadata:
  name: hostel-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hostel
  template:
    metadata:
      labels:
        app: hostel
    spec:
      containers:
      - name: hostel
        image: hostel-app
        imagePullPolicy: Never